#include "function_pointers.h"
#include "3-calc.h"

int opp_add(int a, int b) return (a + b);
int op_sub(int a, int b) return (a - b);
int op_mul(int a, int b) return (a * b);
int op_div(int a, int b) return (a / b);
int op_mod(int a, int b) return (a % b);
