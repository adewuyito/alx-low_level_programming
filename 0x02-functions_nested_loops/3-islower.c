#include "main.h"

/** _islower - check for lower charaachters
 *
 * Return: 1 for lower char or 0 anything else
 */

int _islowe(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	return (0);
}
