#include "main.h"

/**
 * _abs - Computes the absolute val of a number
 * @c: number to be checked
 * Return: Always 0
 */
int _abs(int c);
{
	if (c < 0)
	{
	int abs = c * -1;

	return (abs);
	}
	return (c);
}
