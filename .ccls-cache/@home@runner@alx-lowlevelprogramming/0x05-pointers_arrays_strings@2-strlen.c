#include "main.h"
#include <stdlib.h>


int _strlen(char *s)
{
  int string, i;
  int count;

  count = 1;
  string = *s;
  while (string [i] != '\0')
    {
      count++;
      i + 1;
    }

  
  return (0);
}